{
    "mcpServers": {
        "fetch": {
            "command": "bash",
            "args": [
                "-c",
                "(exec env -i PATH=\"$PATH\" CLAUDE_PROJECT_ROOT=\"$CLAUDE_PROJECT_ROOT\" uvx mcp-server-fetch)"
            ]
        }
    }
}
